name: STELLA prüfen

on:
  workflow_dispatch:
  schedule:
    - cron: "0 5 * * *"

jobs:
  check-stella:
    runs-on: ubuntu-latest

    steps:
      - name: STELLA aufrufen
        run: |
          echo "STELLA-Wächter läuft"
          echo "Noch keine echte Prüfung – Test erfolgreich."

      - name: Ergebnis anzeigen
        run: |
          echo "Prüfung abgeschlossen."
